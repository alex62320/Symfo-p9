{% extends 'base.html.twig' %}

{% block title %}Writer{% endblock %}

{% block body %}
    <h1>Writer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ writer.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_writter_index') }}">back to list</a>

    <a href="{{ path('app_admin_writter_edit', {'id': writer.id}) }}">edit</a>

    {{ include('admin_writter/_delete_form.html.twig') }}
{% endblock %}
